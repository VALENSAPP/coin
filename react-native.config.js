module.exports = {
  assets: [
    // Vector icon fonts are bundled by the RNVectorIcons CocoaPod / Gradle autolinking.
    // Do not link them here — duplicate copies break the iOS build.
    './src/assets/fonts',
  ],
};