import { View, Text } from 'react-native'
import React from 'react'
import { SafeAreaView } from 'react-native-safe-area-context'
const Following = () => {
  return (
    <SafeAreaView style={{backgroundColor: '#f8f2fd',height:'100%'}}>
      <Text>vallowing</Text>
    </SafeAreaView>
  )
}

export default Following