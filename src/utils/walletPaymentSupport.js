/**
 * Shared helpers for creator support / tip recipient resolution.
 * Implementation lives in metaMaskSupport to avoid drift.
 */
export { getSupportRecipientWalletAddress } from './metaMaskSupport';
