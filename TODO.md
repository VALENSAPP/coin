# Task: Add zoom to fullscreen gallery in MyClosetBuyerFlow

## Steps
- [x] Read and understand the codebase
- [x] Get plan approval
- [x] **Edit 1**: Add `SCREEN_HEIGHT` constant for fullscreen image sizing
- [x] **Edit 2**: Add `fullScreenListRef`, `fullScreenScrollEnabled` state, and `handleFullScreenZoomChange` callback
- [x] **Edit 3**: Modify `renderFullScreenItem` to use `InstagramZoomableImage` instead of plain `FastImage`
- [x] **Edit 4**: Update the fullscreen `GestureFlatList` to use `ref`, `scrollEnabled`, and pass `simultaneousHandlers` to items

